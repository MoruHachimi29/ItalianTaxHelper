/**
 * Punto di ingresso principale dell'applicazione semplificato
 * Questo file sostituisce l'architettura client/server separata
 * con un unico processo che serve sia l'API che l'interfaccia utente
 */

// Importa il server
import server from './server.js';

// Avvia il server (il file server.js fa già questo, quindi qui non serve altro codice)
console.log('F24Editabile - Server di avvio semplificato');